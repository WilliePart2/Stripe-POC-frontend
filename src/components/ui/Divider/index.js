import React from 'react';
import PropTypes from 'prop-types';

import './style.scss';

const Divider = () => {
  return (
    <div className="divider" />
  );
};

Divider.propTypes = {

};

export default Divider;
